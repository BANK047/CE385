"# CE385" 

W_1.1
สร้างตัวแปรเพื่อเก็บค่าที่จะแสดง

W_1.2
- สร้างตัวแปรเพื่อชื่อเพื่อสื่อว่าใช้เก็บค่าอะไร 
- แสดงคำตอบแล้วใช้ typeof เพื่อเช็คว่าเป็น type ไหน 

W_1.3 
- สร้างตัวแปรเก็บคะแนนแต่ละวิชา
- สร้างตัวแปรรวมคะแนน ,ค่าเฉลี่ย , ค่าเฉลี่ยมากกว่า 80 เป็นจริง

W_1.4
- สร้างตัวแปรเก็บค่า username = admin , password = 1234 age >= 18
- เช็คเงื่อนไข ถ้าค่าของ username = admin , password = 1234 age >= 18 | ให้ข้าล็อกอินได้
- เงื่อนไข 2 ถ้า ถ้าค่าของ username != admin , password != 1234 | ให้แสดง username or password ไม่ถูกต้อง
- เงื่อนไข 3 ถ้า อายุไม่เกิน 18 แสดง อายุไม่ถึงเกณฑ์
